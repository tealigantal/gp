# Docker Compose 环境变量示例（复制为 .env 后按需修改）
# 注意：不要提交真实密钥到仓库

# 时区（容器与日志使用的时区）
TZ=Asia/Shanghai

# 可选：国内 pip 源（仅构建时使用，加速 docker build）
# 示例：
# PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple
# PIP_INDEX_URL=https://mirrors.aliyun.com/pypi/simple
PIP_INDEX_URL=https://pypi.org/simple

# LLM（可选；/recommend 不依赖，/chat 依赖）
# LLM_BASE_URL：模型服务地址；LLM_API_KEY：访问密钥；CHAT_MODEL：模型名
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=sk-93a2c7ce8bde4cfeb1f9604049fb0469
CHAT_MODEL=deepseek-chat

# 数据提供方（akshare | local | official）
DATA_PROVIDER=akshare

# 路由优先级（可选；默认 AK_SPOT_PRIORITY=em,sina，AK_DAILY_PRIORITY=sina,em,tx）
# AK_SPOT_PRIORITY=em,sina
# AK_DAILY_PRIORITY=sina,em,tx

# HTTP 请求超时时间（秒）
GP_REQUEST_TIMEOUT_SEC=300

# 严格真实数据模式（1 开启/0 关闭；开启后禁用合成降级）
STRICT_REAL_DATA=1

# 动态股票池 + 主线约束（核心过滤参数）
# GP_MIN_AVG_AMOUNT：5 日成交额均值下限
# GP_NEW_STOCK_DAYS：IPO 天数门槛
# GP_PRICE_MIN/GP_PRICE_MAX：价格区间
# GP_DYNAMIC_POOL_SIZE：按成交额选取的候选规模
GP_MIN_AVG_AMOUNT=5e8
GP_NEW_STOCK_DAYS=60
GP_PRICE_MIN=2
GP_PRICE_MAX=500
GP_DYNAMIC_POOL_SIZE=200

# 主线约束设置
GP_RESTRICT_MAINLINE=1
GP_MAINLINE_TOP_N=2
GP_MAINLINE_MODE=auto
GP_MAX_PER_INDUSTRY=2

# 可选：CORS 源白名单（仅设置时启用跨域；逗号分隔）
# GP_CORS_ORIGINS=http://localhost:5173,https://your.site
