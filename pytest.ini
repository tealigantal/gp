[pytest]
testpaths = tests
addopts = -q -m "not integration"
pythonpath = src
markers =
    integration: tests requiring network/LLM/external deps (skipped by default)
    out_of_scope: skipped as non-deterministic or not in current task scope
