# Causal memory maintenance and coherent ranking

## Purpose / Big Picture
Repair stale mature-case maintenance, contradictory score/rank semantics and provider-gap false completion, then rebuild the authorized local GP services. Do not stretch displayed scores or rewrite immutable historical plans.

## Context and Orientation
September 16 audit found memory newest outcome July 21, same 4000-case pool at July22/Sep11, confidence counted twice and provider failures excluded. Existing storage repair and user's conversation edits remain present. Old historical_replay imports deleted pipeline modules and cannot certify current production.

## Progress
- [x] Read operating contracts and independently review memory/ranking boundaries.
- [x] Implement causal worker maintenance with persisted progress, freshness guard, strict observations and actual market breadth.
- [x] Remove provider-gap completion and reopen affected operational runs without deleting their evidence.
- [x] Extract one score kernel, physical-only execution quality, explicit positive net-edge admission, consistent final ranking and versioned identities.
- [x] Run causal, regression and isolated historical comparison checks.
- [x] Rebuild containers and verify data provenance, memory progress, recommendation/no-recommendation and real chat.

## Surprises & Discoveries
Old replay is broken and its future-outcome pending queue is not reusable. Historical long-range price coverage exists but point-in-time full-market membership is not proven. Reconstructed research must not be called formal holdout acceptance.

## Decision Log
Retain existing declared liquidity Top200 evaluation after full frozen main-board coverage; no candidate-source narrowing. Fixed exact-batch Serenity remains signed 3%, lunch remains frozen Top30. Preserve probability estimator for this corrective revision. Remove duplicated confidence from execution quality; keep the explicit 50/30/20 minus downside composite as a heuristic, not probability. Separate positive estimated net return after the repository's 30bps modeling friction from score ranking. All new plans bind new producer/policy and memory content identity. Existing full Adaptive promotion gates are not waived; prospective policy verification and research are reported separately from formal performance acceptance.

## Plan of Work
Worker backfills mature events only, computes breadth from date-truncated history and records actual first-seen. Reader bounds outcome date and knowledge time. Errors remain failed/retryable. New score kernel is shared by production and isolated comparison. Reopen provider-only exclusions and retain their prior evidence as revoked audit history.

## Concrete Steps
python -m pytest -q; explicit memory/scoring/replay tests; compileall; retired-contract check; Compose and diff checks; Docker rebuild and live API/ledger evidence.

## Validation and Acceptance
No future bars or unripe outcomes affect scores; repeated maintenance is idempotent; actual first-seen is immutable; selection ranking matches final score; Serenity cannot promote a nonpositive net-edge candidate; source gaps fail closed. Document observation scope, counts and unresolved upstream gaps. No simulated returns are represented as actual execution.

## Idempotence and Recovery
Append-only events and versioned completed maintenance records; deterministic IDs; per-symbol progress advances after events commit. Existing runtime artifacts are backed up before operational migration. No commits or pushes. Retain old images and source state for rollback.

## Interfaces and Dependencies
Existing Python/Pandas/Numpy/SQLite only. Public historical records remain readable; new policy validation is scoped by revision. No new financial-data service or paid dependency.

## Artifacts and Notes
Previous diagnosis: docs/historical_score_distribution_20260916.md. Reuse existing architecture/product/validation documents.

## Outcomes & Retrospective
Implemented and locally deployed; new producer4 plan and real LLM narration verified. Historical upstream gaps remain explicit, and investment-performance improvement is not established.

### September 17 implementation and deployment evidence

- Full default backend suite: 71 tests passing, including interrupted A / replacement B / resumed A case uniqueness; frontend lint, typecheck, 14 tests and build passed. Compile, retired-contract, Compose and diff checks passed.
- Mature pool reads require complete batch commit time and actual first-seen, then choose the first committed version per symbol/day before date-balanced sampling. Old staged versions never multiply evidence when resumed. Full snapshot coverage is checked before any event write; heartbeat lease prevents competing maintenance writers.
- September 17 real ledger: 3052 raw, 3044 exact daily bars, eight verified official suspensions, zero provider-only exclusions. CNINFO 1225567560 binds 601995 to an explicit September15–17 interval; 1225560951 supplies bounded605303 continuation.
- Frozen historical ablation on the pre-deployment backup: 33 sessions, 6400 scored candidates, 99 old selections. Removing duplicate confidence and applying net-edge admission (holding old probabilities fixed, zero Serenity lane) changes selected mean62.5710 to61.0039 and std1.4230 to1.6018; eight prior selected candidates fail modeled net-edge. This does not prove calibration or future return improvement. New memory is first observed today and cannot be backdated into historical decisions.
- Identified container host-proxy leakage during ordinary build/start; Docker-specific build/runtime proxy variables now default direct, and API health explicitly bypasses proxies. Ordinary compose build succeeds without overrides.
- Offline storage migration: stopping both API and worker, copying entire store into a new Linux volume, hashing every source/destination file, then checking every nonempty SQLite database. Original Windows directory remains intact. This removes Windows shared-filesystem access from active SQLite journals/locks; host/runtime data locations now differ and are documented. Migration completed:900 files /5,140,030,589 bytes matched SHA-256; all nine nonempty SQLite databases passed integrity_check. Both API and worker mounts verified as volume gp-runtime-store-20260917. Original store remains unchanged after cutover. Final new-policy business verification pending.

Historical ledger audit after removing false completion:22 older runs are explicitly retry_wait (15 distinct missing symbols); latest September17 is complete. These old gaps are not fabricated into successful historical data and are not a precondition for the complete current-date publication. No old immutable recommendations are rewritten. Worker recovery continues within its existing retry policy; no claim that every historical market-data gap is repaired.

Live acceptance caught two additional defects after revision3 first published: the narration projection omitted the new return/cost facts, and the existing close-anchored stop could sit inside the entry zone. Fixed projection and prompt; stop is now below the lowest permitted entry and reward/risk uses the highest entry. Producer revision4 prevents reuse of the immutable revision3 plan. Full backend suite now72 passing tests, including the actual narrator evidence boundary and trade-plan price ordering. The first diagnostic conversation was deleted. A subsequent official-source recheck could parse both PDFs but exchange verification was unavailable; it correctly returned no new fact, without changing the earlier successfully verified ledger evidence.

## Final acceptance

2026-09-17 final causal repair and container migration acceptance: API/worker/web rebuilt; both backend services match all198 workspace Python source hashes. Active /app/store is external Linux volume gp-runtime-store-20260917. Offline migration preserved900 original files (5,140,030,589 bytes), verified every SHA-256, and all9 nonempty SQLite databases passed integrity_check. The original host store is rollback evidence, no longer live. Build/runtime proxy scopes are separate and ordinary Compose rebuild/start succeeds.

Mature maintenance completed all3052 symbols with364436 new mature cases. Current4000-case pool covers2145 symbols, all four regimes, latest signal2026-09-09 and outcome2026-09-16; staging is invisible and first-committed case versions are unique. Live source akshare:sina returned5564 rows; reconstructed same-session mainboard denominator3052, exact bars3044 plus8 verified suspensions, zero provider-failure exclusions. Liquidity Top200 produced199 scored candidates;601091 has only1 historical row (80 required). The reconstructed capture remains explicitly approximate; it is not proof of historical14:57 membership.

Final publication publication_c4e216b1850036accadc5ae2 / plan_6456cfcd2e311a3c2af184e1 uses daily producer4, evidence2026-09-17 and session2026-09-18. Exact Serenity batch is active at3%. Scores across199 candidates:36.0617–63.3872, mean48.7616, std5.2910; selected scores60.6194/59.9459/59.5640. All final ranking scores equal displayed scores, selected net edges are positive, and selected stop prices are below their entire entry zones. Actual nginx→/api/chat→LLM explains net estimates0.0915%/0.4782%/0.0497%, modeled0.3000% cost, and why a higher-score candidate with negative net edge is not selected. Health/chat accurately say future session, currently post-close and not executable. Both diagnostic sessions were deleted (204).

Validation:72 default backend tests, frontend lint/typecheck/14 tests/build, compile, retired-contract check, Compose and diff checks passed.22 older market runs remain explicitly retryable after false provider exclusions were revoked; they are not certified complete or rewritten. No claim of improved returns or calibration. Full artifacts:results/causal-repair-20260917/{runtime-volume-migration,live-verification,final-health,final-chat-repaired,source-hashes,frozen-score-comparison}.json. No commit or push.
