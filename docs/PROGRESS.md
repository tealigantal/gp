# Current Progress

Last updated: 2026-08-02

2026-08-02 official one-day suspension recovery: diagnosed the live 2026-07-31 coverage residue as `600439` after 76 daily-source attempts. CNINFO announcement `1225449756` and the SSE verifier prove an all-day 2026-07-31 halt with 2026-08-03 resumption as `ST瑞贝卡`; PDF parsing succeeded, but the exact-date matcher accepted only wording containing “开市/开盘”. The matcher now also accepts an exact “停牌日期” field or an exact target-date “停牌1天/一天/全天” statement while retaining pre-open publication, exchange verification, parsed-PDF and same-date resume rejection gates. Container reconstruction then exposed a second causal defect: a Friday 23:58 failure due at Saturday 00:03 was never retried because closed-session `tick()` returned before historical recovery scheduling. Closed days now continue frozen historical recovery and next-session base publication while same-session freezing, lunch and intraday work stay disabled. Targeted tests, the 52-test default backend suite, compilation and a live read-only collector probe passed before the final reconstruction.

2026-07-28 tail-entry narration implementation: the user explicitly chose a prompt-led manual tail-entry coach rather than an automatic execution engine because live data cannot currently support auditable automated decisions. The sole production `ConversationService` now supplies a fixed 14:45–14:56 manual checklist and each candidate's bound daily signal label to the LLM: price range, VWAP, relative CSI300 strength, tail volume ratio at least 1.3, recent five-minute shape, no-chase and stop discipline. It requires the model to tell the user to inspect their own chart and forbids invented live price, VWAP, volume, relative-strength or pass/fail claims; it does not add a source, worker lane, runtime schema, selection change or order action. The existing chat contract test was extended without changing test count; compilation, all 52 backend tests, retired-contract check, Compose validation, frontend lint/typecheck/14 tests/build and diff check passed. `gp` and `gp-worker` were rebuilt/recreated without changing the web container or any data volume. A disposable real `/api/chat` request for 601727 returned the plan-specific 6.85–6.9185 range, VWAP, CSI300 relative-strength, volume-ratio≥1.3, five-minute, no-chase and 14:57-cutoff conditions, explicitly asking the user to inspect their own market screen; its session DELETE returned 204.

2026-07-28 completed current-day reconstructed-run repair: this was neither a database regression nor a Serenity or official-announcement failure. The 14:57 freeze had been missed, so the live 2026-07-28 run was correctly marked `reconstructed_current_universe`; an implementation error then incorrectly disabled the independent same-day fresh-zero snapshot fact. Live Sina evidence at 20:39 was target-session, non-fallback, non-stale, field-complete and all-zero for `002036` / `002828`, with positive prior closes. The orchestrator now preserves the approximate provenance but consumes that fact only for the current target day, immediately rereads exact persisted bars after reconciliation, and never permits it for historical or cross-day recovery. The rebuilt worker completed 3042/3042 at 20:41, stored both exclusions as `trusted_no_trade`, and published `plan_3b7563…` / `publication_9c869b…` for session 2026-07-29 at 20:43. The live Chinese workspace shows “下一交易日计划已发布”, 2026-07-28 evidence and waiting-for-open; a disposable real chat returned those same date and tradeability facts and its session was deleted. Backend 52 tests, frontend lint/typecheck/14 tests/build, Compose validation and screenshot acceptance passed.

2026-07-28 completed daily-fetch throughput repair: the 900-second whole-market budget introduced by the market-day rewrite was a new orchestration defect, not a Serenity or database failure. It cut a healthy persisted scan into an artificial five-minute retry wait. The same path also used `substr(item_time,...)`, which bypassed the existing `(query_id,item_time)` history index, and rewrote all 3,044 ledger rows after each 100-symbol batch. The child now finishes every unattempted batch, exact coverage uses an indexed half-open target-date range, and the ledger writes only the observed batch. The existing daily test was extended without changing test count to prove a 101st symbol still runs after the old deadline and partial ledger synchronization retains earlier fetched state. In the rebuilt real container, 3,044-symbol exact readback completed in 2.716 seconds; the next real retry touched only the two unresolved symbols and emitted the exact `3042/3044` progress directly after their bounded source routes. All backend 52 tests and frontend lint, typecheck, 14 tests and production build passed. `gp` and `gp-worker` alone were rebuilt and recreated; `web` and data volumes remained running and untouched.

2026-07-28 completed publication-boundary hardening: the cross-midnight repair was re-audited for a separate 09:30 race. Previously a full-market calculation admitted at 09:29 could have completed after 09:30 and still moved the public pointer. The worker now rechecks the same base-plan window immediately before publication; a late computation stays only as an unpinned immutable audit plan and emits `generation_window_closed`. Serenity target reconstruction or decision lookup errors no longer disappear in the same decision branch: the valid 0% base remains current and the worker emits `serenity_upgrade_skipped`. The existing daily-orchestration test directly simulates a 09:29 start and a 09:30 completion without changing test count. The relevant 17 tests, full 52-test backend suite, compilation and diff check passed. Only `gp` and `gp-worker` were rebuilt; live inspection of the recreated worker confirmed both guards are present, health remained on `publication_ec20…` / `plan_d11c…`, and all containers stayed running.

2026-07-28 completed repair: the 2026-07-27 run is no longer treated as a database fault or a Serenity batch failure. After a real worker retry, normal `sina → em → tx` daily sources failed for only `002036` and `002828`; the independent official collector then verified CNINFO `1225440686` / `1225441689`, matching Shenzhen announcement IDs and target-date PDF halt wording. The run completed at 00:35 with raw universe 3044, expected coverage 3042, and both `official_suspension` evidence records stored in `market_runs.db`.

2026-07-28 completed cross-midnight repair: inspection found a second, separate scheduling defect. Although 2026-07-27 daily evidence was exactly 3042/3042, the old base-plan window accepted only 15:20–midnight or 09:20–09:30. The 00:35 recovery completion therefore left `ready_to_publish` while the current pointer remained the old 2026-07-27 plan using 2026-07-24 evidence. `MarketClock` now allows the next-session plan from 15:20 through the target session's 09:30, including overnight recovery. After rebuilding only `gp-worker`, `gp` and `web`, the worker acquired its expired lease, generated and published `plan_857c…` / `publication_09b756…` at 01:14. Live health now reports plan session 2026-07-28, daily evidence 2026-07-27 and `next_plan_target=published`; the old plan is no longer current. The browser and chat identify a non-published next plan precisely and identify old candidates as “上一份完整计划（仅回顾）”.

2026-07-28 completed Serenity successor repair: the new 2026-07-28 base plan correctly published first at 0% while the independent announcement batch was pending, but the prior same-date short circuit would have stranded it there after the batch completed. The worker now permits exactly one matching 0% → 3% successor after `load_decision` proves an atomically complete target. The deployed worker produced `plan_d11c…` / `publication_ec20…` at 01:27 with active 3% Serenity, the same 2026-07-28 session and 2026-07-27 daily evidence, and exactly three selected candidates. The final live chat and desktop/mobile pages bind to that publication.

The browser and chat now consume two server-clock projections rather than using a publication's historical RuntimeObservation as the current market state: `health.market_now` describes the current publication, while `health.next_plan_target` describes the independently resolved next target session, required daily evidence and recovery/publication state. The current-plan panel labels a post-close expired plan as “仅供回顾”; a separate next-plan card says “生成中” with target/evidence/coverage when 3042/3044 is incomplete, or “已发布，等待开盘” once the next plan exists. An expired old plan therefore never means tomorrow's plan is forbidden. After authorization, only `gp` and `web` were rebuilt and recreated; `gp-worker` remained running and data volumes were untouched. The live health response at 23:29 resolved 7/28 as the next target, 7/27 as required evidence, and 3042/3044 with two failures. Browser verification found both cards and no error overlay. A disposable real chat reached the new API but the external model returned 503; its session was deleted, so model availability remains a separate operational issue.

The actual chat path has been reduced to one narrator: `ConversationService → LLMClient.chat`. The unreferenced old `llm/narrate.py` module and the disconnected `tools/legacy/` router sources were physically removed, together with code used solely by that dead path; scoring, Serenity, persistence and the current session/publication contracts were retained. A deterministic answer-time header now distinguishes the answer clock from historical runtime observation and prevents an ended plan being presented as tomorrow's plan. Chat and health read the market-run ledger without initializing it. After rebuilding only `gp` and `gp-worker`, a disposable real chat at 22:39 correctly reported the market closed, the 2026-07-27 plan ended, recovery at 3042/3044 and no stale 14:59/next-day claim; the session was deleted. The default suite remains 52 passing tests. See `docs/plans/2026-07-27-single-chat-narration-temporal-truth.md` and ADR 0014.

The worker has been rewritten around a durable market-day ledger rather than the former 60-second/30-minute condition chain. `store/market_runs.db` records frozen denominators, symbol-level exact-date coverage, retry/error evidence, recovery leases and lunch completion; it does not alter `history.db`, `agent.db`, current recommendation contracts or Serenity's atomic 3% rule. The daily producer is now read-only after an exact completed run, and both the publication service and database boundary refuse incomplete/pending plans. API refresh endpoints are worker-owned refusals rather than hidden collection paths. Health and the Chinese workspace display recovery state while keeping the last complete recommendation and its new-chat binding. Full test evidence is recorded below; container recovery observation remains the active operational checkpoint in `docs/plans/2026-07-27-market-day-orchestration-self-healing.md` and ADR 0013.

The local container observation is now live: exact target-date readback found 3042 of 3044 symbols for 2026-07-27 in 1.834 seconds, so the new worker retried only `002036` and `002828`, recorded their route failures and scheduled the next retry. The valid 2026-07-27 publication remains current, while health and the rebuilt workspace clearly show recovery. The external-source gap is deliberately still pending rather than being relabeled as a complete plan.

The 2026-07-27 daily-K incident was traced to two worker/provider contracts, not database corruption and not Serenity: the lunch-rerank change treated “not a lunch plan” as permission to refresh the full market in the afternoon, and the AkShare timeout wrapper held a global lock for each network call, making configured batch concurrency serial. The worker now binds refresh eligibility to the current target's market-session and daily-evidence dates, suppresses full-market refresh in afternoon/closing auction, and retries after close only when that target is not complete. The request policy is context-local, so it retains AkShare-only timeout/header enforcement without serializing concurrent requests. The existing daily-refresh test was revised rather than expanded. See `docs/plans/2026-07-27-worker-daily-refresh-gate.md`.

The authorized local rebuild of only `gp` and `gp-worker` completed. `gp` is healthy, `gp-worker` has no restart, its Serenity child remains separate, and the running worker contains the exact repaired `cli.py`; `web` was left running unchanged. The old current recommendation remains visible while post-close target-date recovery proceeds, which is the intended single recovery path rather than an afternoon retry loop.

The retirement checker now exempts only the explicitly marked 2026-04-01 historical repair brief from its active-contract vocabulary scan. Its preserved historical terminology no longer blocks the current suite, while every current source and document remains checked.

Documentation has been consolidated under `docs/README.md`. The map separates current authorities, execution records, decision records, and historical archive material; ADR and archive indexes now resolve only to retained files. The dated 2026-04-01 repair brief was moved out of the repository root into `docs/archive/history/` and explicitly marked historical. Local Markdown link and whitespace checks passed; this documentation-only maintenance did not change the runtime.

Exact target-date daily coverage and bounded Serenity OCR are implemented. The raw full-market count is preserved, a no-bar symbol is excluded only by same-session fresh spot evidence, readiness requires 100% exact-date coverage, and the next worker refresh is measured from actual completion. Serenity prefilters unsupported revision titles and uses resource-bounded Tesseract Chinese OCR only for structurally valid zero-text PDFs; any uncertainty retains the atomic 0% result. Default and targeted tests, compilation, Compose validation, an isolated image build, dependency inspection and a real synthetic Chinese scan passed. See `docs/plans/2026-07-24-daily-refresh-and-serenity-ocr-recovery.md` and ADR 0012.

The user subsequently authorized local container reconstruction. After an initial diagnostic deployment exposed a legacy spot pickle without provenance, the follow-up image `sha256:85b3dde...` from commit `0e2cfa2` ran in `gp` and `gp-worker`; `web` and all data volumes were preserved. The cache requires a matching digest, approved live source, timezone-aware capture time and exact session date; TTL uses that capture time rather than file mtime, and no disk result—fresh or fallback—is promoted into process memory. The real new-container probe identified `002036` as the only explicit no-bar symbol, kept the raw main-board count at 3044, achieved exact 3043/3043 target-date coverage and published a new Top-3 recommendation. Real chat, schema and integrity checks passed; this is recorded historical validation evidence, not a current PR-status claim.

The immutable 11:30 five-minute rerank of the base plan's frozen Top-30 completed local implementation and validation. No ContractModel, HTTP-shape, or SQLite-schema changed: the base plan remains readable, a complete batch appends a new plan/runtime/publication, and any incomplete or failed batch leaves the morning current publication untouched. The default 40-test suite, three explicit minute-provider tests, one live isolated-source integration test, compile/contract/retirement/Compose/diff checks, and independent read-only review are the recorded acceptance boundary. No container was rebuilt or replaced for that validation. See `docs/plans/2026-07-24-lunch-five-minute-rerank.md` and ADR 0011.

Serenity has been restored as an atomic fixed-3% official-announcement expert in the current contract kernel. The base engine freezes Top-30 first. An exact complete batch applies a bounded signed contribution; any missing candidate, source/PDF failure, stale state, or mismatch makes the whole batch zero without changing the base recommendation. Collection is supervised as a separate process inside `gp-worker`, while plan and chat paths remain network-free. Serenity batch identity now participates in plan identity, so later complete evidence can replace the stable zero plan without error-driven churn.

The latest local deployment uses one shared backend image for `gp` and `gp-worker`; the obsolete standalone Serenity container has been removed without deleting its mounted historical evidence. The live source run currently fails closed because a `000100` PDF is unparsed, so the current plan correctly records 0% Serenity contribution for all 30 finalists while the core plan/runtime loop continues.

The narration system prompt now explains Serenity's official-announcement role, fixed 3%, ±0.03 bound, complete-neutral behavior, and whole-batch zero rule. A real LLM response described those principles and the current zero effect in natural Chinese without exposing field names, reason codes, internal IDs, or engineering interfaces.

The contract-kernel hard cutover is implemented and locally validated. The configured development database now has canonical plan, runtime, publication, session, and turn records produced through the new-only path. The unified worker owns offline daily-evidence refresh, plan generation, and runtime refresh; it never runs from `/api/chat`.

The chat-first frontend has been rebuilt as a minimal responsive workspace against current publication, health, conversation, and chat contracts. Plain `docker compose up -d --build` now reproducibly builds the Windows-authored lockfile in Alpine, waits on API health, and starts healthy `gp`, `gp-worker`, and `web` services. Browser verification covered current candidates, a real LLM reply, saved-session reload, and zero captured console errors.

The frontend now tolerates minute-level runtime publication rollover without confusing it with a changed daily plan. Current and conversation-bound publications remain separate, health/publication reads are version-checked, unknown lineage fails closed without a false historical claim, reason codes render in Chinese, and candidate metrics use `进入评分` plus `execution_risk`. The deployed local web container passed a real follow-up on the previously affected `a7d44e66` session; backend services were not rebuilt or restarted.

Saved conversations now support confirmed permanent deletion from each sidebar card. Deletion is scoped to one session and its messages, cannot remove recommendation artifacts, cannot be undone by stale browser responses, and cannot be resurrected by a delayed same-ID chat request. The rebuilt local services passed an end-to-end disposable-session deletion; the eight pre-existing conversations remained intact.
