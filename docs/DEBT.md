# Deliberate Debt

None recorded for the contract-kernel cutover. The migration discards retired rows that cannot map exactly instead of introducing compatibility debt.

2026-07-24: The former Serenity v1 no-OCR availability debt is resolved by the resource-bounded v2 lane. Malformed, encrypted, truncated, oversized, timed-out, low-confidence or inconsistent documents still make the exact batch 0%; multi-page production soak remains operational evidence to collect, not permission to weaken the atomic gate.

2026-07-24: Lunch reranking currently has one verified free primary five-minute route and no source SLA. The design deliberately treats any timeout or incomplete symbol/benchmark window as an atomic unavailable batch and preserves the morning publication, so this is availability debt rather than recommendation-integrity debt. Multi-day forward soak is still required before claiming operational source stability; a future second route must pass whole-batch validation and must never be mixed per symbol.
