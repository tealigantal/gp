# Validation Ledger

2026-07-28 manual tail-entry narration: no test file or test count was added. The existing `tests/contracts/test_contract_lifecycle.py` now proves the actual production narrator receives a 14:45–14:56 manual tail-entry rule, a tail-volume threshold of 1.3, a ban on invented real-time facts, and each bound candidate's daily signal label. `python -m compileall -q src tests`, the revised five-test contract file and full `python -m pytest -q` suite (52 tests) passed. With the repository command `PYTHONPATH=src python -m gp_assistant.contracts.check_retired`, the retired-contract check passed; `docker compose config --quiet` and `git diff --check` passed. Frontend source is unchanged but its existing lint, typecheck, 14-test Vitest suite and production build passed. `docker compose up -d --build gp gp-worker` rebuilt/recreated only API and worker, leaving `web` and data volumes intact; `gp` became healthy and worker started. A disposable live `/api/chat` request asked how to monitor 601727 tomorrow afternoon. It returned the immutable 6.85–6.9185 entry range, 6.558 stop and 7.153 target, then the manual VWAP, CSI300 relative-strength, tail-volume≥1.3, three-five-minute-bar, no-chase and 14:57 cutoff conditions. It explicitly required the user to check their own screen and did not invent any live indicator value. The verification session was deleted with HTTP 204. Remaining risk: these are fixed user-operated narration rules, not statistically validated automatic signals or live pass/fail observations.

2026-07-28 current-day reconstructed-run repair: no test file or test count was added. The existing `tests/contracts/test_daily_refresh_exact_coverage.py` now proves that a current-day approximate run with a fresh exact-session full-zero snapshot excludes only the verified no-trade symbol, preserves `approximate=True`, and keeps a historical reconstructed target fully expected. It also exercises the immediate post-reconciliation exact-bar reread. The revised file passed 5 tests; `python -m compileall -q src tests`, full `python -m pytest -q` (52 tests), retirement-contract check, Compose validation and `git diff --check` passed. Frontend lint, typecheck, existing 14-test Vitest suite and production build passed. After rebuilding/recreating only `gp`, `gp-worker` and `web` without data-volume deletion or orphan cleanup, real 2026-07-28 recovery observed a fresh `akshare:sina` target-session snapshot: `002036` and `002828` each had positive `prev_close` and zeros for price/OHLC/volume/amount. The worker stored both as `excluded / trusted_no_trade`, preserved the run's reconstructed provenance, reread exactly 3042 persisted bars, completed at `20:41:25+08:00`, and published session 2026-07-29 with 2026-07-28 evidence. `/api/health`, `/api/recommendation/current`, a disposable real `/api/chat` response (then DELETE 204), and a browser desktop screenshot all agreed: future plan, 2026-07-28 evidence, waiting for the 2026-07-29 open, recovery ready. Console errors/warnings were empty.

2026-07-28 daily-fetch throughput repair: the existing daily orchestration contract test was extended without adding a file or test count. It proves a 101-symbol run continues into the final batch even when its injected clock has passed the removed 900-second boundary, and that a partial ledger synchronization updates only the newly observed symbol without regressing an earlier fetched row. The targeted daily/Serenity contract run passed 17 tests; the full backend suite passed 52; compilation, retired-contract checking, Compose validation and `git diff --check` passed. Frontend lint, typecheck, the existing 14-test Vitest suite and the production build passed. After rebuilding and recreating only `gp` and `gp-worker`, in-container inspection confirmed the total budget is absent, batch-only ledger synchronization and indexed date-range coverage are present. A live read-only 3,044-symbol exact readback for 2026-07-28 returned 3,042 rows in 2.716 seconds; its next due retry fetched only `002036` / `002828`, exhausted `sina → em → tx`, and then emitted exact `3042/3044` progress. Health retained `publication_ec20fff78f5174a7a15dce5a` / `plan_d11c2569a0390f4706a87c52`; no data volume, current pointer or frontend container was modified.

## Official Suspension Evidence for Daily Coverage

2026-07-28 publication-boundary hardening: the existing daily-orchestration test was extended without increasing its count to simulate a valid 09:29 admission followed by a 09:30 completion. It proves the producer may finish its immutable plan but `current_publication` remains unchanged. The revised daily and Serenity contract files passed 17 tests; the full backend suite passed 52 tests; `python -m compileall -q src tests` and `git diff --check` passed. After rebuilding and recreating only `gp-worker` and `gp`, in-container source inspection confirmed both `generation_window_closed` and `serenity_upgrade_skipped` guards. `/api/health` remained bound to `publication_ec20fff78f5174a7a15dce5a` / `plan_d11c2569a0390f4706a87c52`, session `2026-07-28`, evidence `2026-07-27`, atomic Serenity 3% ready state, and recovery `ready`; `gp`, `gp-worker`, and `web` were running. No data volume, plan, publication, or frontend artifact was deleted or changed by this deployment.

2026-07-28: No test file or test count was added. `python -m compileall -q src tests`, the revised existing `tests/contracts/test_daily_refresh_exact_coverage.py` (5 tests), and the full `python -m pytest -q` suite (52 tests) passed. The modified existing test proves that normal daily data remains primary, an interrupted worker retries only the remaining symbol, an injected verified official fact completes the exact-date run only by writing `excluded / official_suspension` plus auditable evidence, and an announcement whose PDF names a different target date is rejected. A read-only live run of the new collector returned `002036 → 1225440686 → fd5e957fa302a3fe` and `002828 → 1225441689 → b0a1d78dfb5b208f`, both with Shenzhen announcement-ID verification and effective date 2026-07-27. `python -m gp_assistant.contracts.check_retired`, `docker compose config --quiet` and `git diff --check` also passed.

2026-07-28 real container recovery: the first replacement did not contain the new module because `gp-worker` consumed the shared backend image while only `gp` owned the Compose `build` declaration. The build declaration was moved to the shared service definition; `docker compose build gp-worker` then produced image `sha256:454ecf…`, and only `gp-worker` was recreated. Its real run first exhausted `sina → em → tx` for the same two missing symbols, then wrote both verified official suspension records into `store/market_runs.db`. The run completed at `00:35:30+08:00`; raw universe remained 3044, expected universe was 3042, `002036` / `002828` were `excluded / official_suspension`, and their CNINFO IDs, document hashes, URLs, pre-open timestamps, PDF excerpts and Shenzhen ID-verification basis were persisted. `/api/health` returned recovery `ready` and next target `2026-07-28 / ready_to_publish`; `gp` and `web` stayed healthy and were not recreated.

2026-07-28 source-level scheduling correction: a read-only store check found latest daily date `2026-07-27`, exact coverage `3042/3042`, and a current publication whose plan was generated from `2026-07-24` evidence for the `2026-07-27` session. The mismatch was not a bad daily fetch: the recovery completed at 00:35 after the old 15:20–midnight window had closed and before its 09:20 retry slot. The revised existing five-test daily-orchestration suite now asserts that plan generation is eligible at 15:20, 00:35 and 09:29, but not at 09:30 or 14:59. The full backend suite (52 tests), compilation and diff check passed. This is source-level evidence only; no new image, plan, publication or runtime data has been written.

2026-07-28 real cross-midnight recovery: after explicit authorization, only `gp-worker`, `gp` and `web` were rebuilt and recreated; no data volume or unrelated container was removed. The new worker first respected the unexpired worker lease, then acquired it and reread the completed 2026-07-27 run at exact 3042/3042 coverage. It produced and published `plan_857c9d7e…` / `publication_09b75626…` at `01:14:28+08:00`. Live `/api/health` then reported current plan session `2026-07-28`, daily evidence `2026-07-27`, and next-target state `published`; `/api/recommendation/current` returned the same new publication with 197 evaluated candidates and three selected. A real temporary `/api/chat` request returned the deterministic pre-open notice plus the exact plan/evidence dates and was deleted with 204; no verification session remains.

2026-07-28 real Serenity successor: live inspection found that the initial new plan had 0% while the isolated worker had already completed its compatible announcement batch, exposing an unconditional same-date publication short circuit. The revised existing Serenity identity test now proves a matching ready batch authorizes a single 0% → 3% successor while an already-active plan cannot repeat it. The revised existing Serenity and daily-orchestration tests (17 total), full backend suite (52), compilation, frontend 14-test suite, lint, typecheck, production build, retirement check, Compose validation and diff check passed. After rebuilding only `gp-worker` and `gp`, the live worker published `plan_d11c2569…` / `publication_ec20fff7…` at `01:27:47+08:00`; the plan retained session `2026-07-28` and evidence `2026-07-27`, carried active Serenity weight `0.03` with a committed batch reference, and had three selected candidates. A final real chat returned 200 from that exact publication, correctly reported the pre-open gate and dates, and its disposable session was deleted with 204.

## Frontend Screenshot Follow-up

2026-07-28 source-level browser acceptance: the running web image rendered a meaningful desktop and 390×844 mobile page, showed no framework error overlay or console error, and had no mobile horizontal overflow. The screenshot also correctly displayed completed 2026-07-27 daily evidence, but exposed the real scheduling defect: it kept a plan with 2026-07-24 evidence visible after the exact 2026-07-27 run completed overnight. The source UI now labels that panel “上一份完整计划（仅回顾）” and labels the candidate section as not being the next-session plan; the next-plan card says that evidence is complete but a new version has not been published. The existing 14-test Vitest suite, `npm run typecheck`, `npm run build`, `python -m pytest -q` (52 tests), compilation and diff check passed. The corrected UI remains pending authorized `gp`/`web` container rebuild and repeat screenshot acceptance.

2026-07-28 deployed browser acceptance: at desktop width the real web page displayed “当前计划简报”, plan date `2026-07-28`, evidence `2026-07-27`, “下一交易日计划已发布” and the new selected candidates. At 390×844, the same new plan card and candidates were readable after normal document scrolling, with no horizontal overflow; the page had content, no framework error overlay and no console errors. These checks read the deployed API, not a mocked fixture or stale browser cache.

## Frontend Current-Time Projection

2026-07-27 follow-up correction: existing contract tests were revised, not expanded. `python -m compileall -q src tests`, the full `python -m pytest -q` suite (52 tests), frontend lint, typecheck, the existing 14-test Vitest suite, production build, `docker compose config --quiet` and `git diff --check` passed. The revised assertions prove that a post-close current plan is review-only while the separate 2026-07-24 next target still requires 2026-07-23 evidence and remains pending at 3042/3044. A read-only projection against the current stores at 23:26 resolved `market_session_date=2026-07-28`, `required_daily_evidence_date=2026-07-27`, `state=pending_daily_evidence`, coverage `3042/3044`, failed `2`, and approximate-universe `true`. This is source-level validation only; no container or runtime data was changed by this follow-up correction.

2026-07-27 deployment verification: after explicit approval, `docker compose build gp web` and `docker compose up -d --no-deps gp web` rebuilt/recreated only API and web; `gp-worker` stayed up and no data volume was touched. Live `/api/health` returned the same 7/28 target, 7/27 required evidence, 3042/3044, two failures and `pending_daily_evidence`; both new containers became healthy. Browser verification at port 8080 found non-empty content, no framework error overlay, and the exact Chinese card facts “当前发布计划已结束”, “下一交易日计划生成中”, “目标交易日 2026-07-28” and “已完成 3042/3044”. A disposable live `/api/chat` reached API but returned 503 from the external model service; the disposable session was deleted with 204, so this deployment validates the new UI/API projection but does not claim external-model availability.

2026-07-27: Modified existing backend/frontend tests only. `python -m pytest -q` passed 52 tests; `python -m compileall -q src tests`, retirement-contract check, `docker compose config --quiet` and `git diff --check` passed. From `frontend/`, lint, TypeScript check, the existing 14-test Vitest suite and production build all passed. The revised tests prove the health shape includes `market_now`, the server-clock projection turns a post-close same-day plan into `expired`/non-executable, and the workspace renders “当前计划已结束”, “仅供回顾” and recovery coverage rather than historical closing-auction execution as the current state.

2026-07-27 container/browser evidence: rebuilt and recreated only `gp` and `web`; both are healthy while `gp-worker` remained running and data volumes were untouched. The running API source hash matched the workspace. At 23:00, `/api/health` still reported the immutable publication's historical `closing_auction`/`tradeable` fields, while `market_now` accurately reported `postclose`, `expired` and `tradeable_now=false`; this is intentional compatibility plus the new authority boundary. The real browser page showed “市场已收盘 · 计划仅供回顾”, plan date 2026-07-27, evidence 2026-07-24, recovery 3042/3044 with approximate-universe context, and no browser warnings or errors.

## Single Chat Narration and Temporal Truth

2026-07-27: Changed existing contract tests only; the default `python -m pytest -q` suite passed 52 tests. `python -m compileall -q src tests`, `docker compose config --quiet`, the retirement-contract check and `git diff --check` also passed. The revised existing tests prove that an after-close response labels an old plan as review-only, identifies a stale runtime observation as historical, preserves one deterministic time notice, rejects a model's false current-time closing-auction claim before persistence, and does not create `market_runs.db` during a chat read.

2026-07-27 container evidence: rebuilt and recreated only `gp` and `gp-worker`; `gp` is healthy, `gp-worker` is running, and `web` was left unchanged. The local and running-container source hashes matched. A disposable real `/api/chat` conversation at 22:39 returned a single answer-time notice: Shanghai market closed, the 2026-07-27 plan had ended and was review-only, market recovery was 3042/3044 with two failures. The full reply contained neither the historical 14:59 as current time, a false post-close publication claim, nor a statement that the expired plan was for tomorrow's opening. The disposable session delete returned 204. No runtime data volume, recommendation plan, publication, scoring path or Serenity evidence was deleted.

## Market-Day Orchestration and Self-Healing

2026-07-27: `python -m compileall -q src tests`, `python -m pytest -q` (52 tests), `docker compose config --quiet`, frontend typecheck/lint/14-test Vitest run/build, and `git diff --check` passed. Existing tests were revised rather than expanded: they now prove target-date rather than latest-row coverage, interrupted symbol-level recovery, trusted same-day suspension exclusion, frozen-universe plan production without a spot poll, post-close source probing without a full scan, publication-pointer safety, and persisted lunch reuse after a simulated worker restart.

2026-07-27 container evidence: rebuilt `gp` and `gp-worker` repeatedly only as required by discovered runtime fixes, then rebuilt `web` alone with `--no-deps`; no data volume or orphan cleanup was used. All three services are healthy/running. `/api/health` returned 200 in 20 consecutive reads before the final store retry repair and 10 consecutive reads after it; browser verification at port 8080 found content, the Chinese recovery notice, no framework error overlay, and interactive controls. The restarted worker reclaimed its expired heartbeat lease, created/reused the current recovery run, read exact coverage in 1.834 seconds, marked 3042/3044 covered, attempted only `002036` and `002828` through `sina → em → tx`, and recorded two failures plus a five-minute next retry. The 2026-07-27 complete publication remains current; no incomplete recovery plan or runtime moved the pointer. This is live proof of fail-closed recovery and retry, not a claim that the two external source failures have already been repaired.

## Worker Daily-Refresh Gate Repair

2026-07-27: `python -m pytest -q tests/contracts/test_daily_refresh_exact_coverage.py` passed 5 tests and `python -m compileall -q src tests` passed. The revised existing worker test proves a valid base plan does not initiate a daily refresh in the afternoon when no lunch rerank exists, post-close retries when the next-session target is absent, and a bound next-session target does not repeat. A no-network two-thread request probe observed `peak_concurrent_requests=2` through the AkShare timeout wrapper, proving the request policy no longer holds its global lock across network execution. `git diff --check` passed.

2026-07-27: the full default `python -m pytest -q` run initially reached the suite and failed because the retirement checker scanned `docs/archive/history/gp_repair_list_20260401.md`, a file explicitly preserved as historical evidence and marked not-current. The narrow exception now excludes only that archived brief; it does not restore any retired code or contract. `python -m gp_assistant.contracts.check_retired` and the rerun full suite both passed (52 tests).

2026-07-27 container reconstruction: `docker compose config --quiet`, `docker compose build gp gp-worker`, and `docker compose up -d gp gp-worker` completed without `--remove-orphans`. `gp` became healthy; `gp-worker` stayed running with zero restarts and retained its separate Serenity child process; `web` was not rebuilt or restarted. The local and running-container SHA-256 for `src/gp_assistant/cli.py` matched (`2a83448e...`). `/api/health` continued to serve the existing valid 2026-07-27/2026-07-24 publication with Serenity ready. The worker was observed post-close; the existing publication is intentionally not treated as the next-session daily target, so one target-date recovery attempt is allowed rather than being misclassified as the prior afternoon loop.

## Markdown Documentation Consolidation

2026-07-24: Inventoried 47 tracked Markdown files outside ignored runtime paths. Added `docs/README.md` as the navigation map, repaired the ADR and archive indexes, and moved the retired 2026-04-01 repair brief into the historical archive with an explicit warning. A PowerShell local-link scan reported `NO_BROKEN_RELATIVE_MARKDOWN_LINKS`; `git diff --check` passed. This validation changed no application code, contract, dependency, schema, container, or runtime artifact.

## Exact Daily Coverage and Serenity OCR v2

2026-07-24: `python -m pytest -q` passed 47 default tests after the final OCR whitespace regression was added. Targeted OCR/source and daily-refresh/lunch/Serenity contract suites passed; `python -m compileall -q src tests`, `docker compose config --quiet`, contract/retired-symbol checks and `git diff --check` also passed. Tests prove old-date nonempty responses stay pending, only fresh same-session complete zero rows can be excluded, resumed symbols reenter the denominator, only missing symbols refresh, exact coverage must reach 100%, and both successful and failed scans wait a full monotonic interval from completion.

2026-07-24: Built isolated image `gp-backend:ocr-pr10` without recreating any service. The image reported Tesseract 5.3.0 with `chi_sim`, `eng`, and `osd`; pypdfium2 5.12.1, pytesseract 0.3.13, and Pillow 12.3.0 matched the pinned versions. A generated one-page Chinese image-only PDF passed the actual container parser as `tesseract_ocr`, with symbol/topic checks, 93 median confidence, and consistent 200/300 DPI percentage recognition. Corrupt, page-limited, low-confidence, symbol/topic mismatch and inconsistent-number paths are covered by deterministic tests and map the entire exact Serenity batch to 0%. No third-party PDF or runtime store artifact is part of the change.

2026-07-24 deployment check: after explicit user authorization, rebuilt shared backend image from commit `48e7cb3` and recreated only `gp` and `gp-worker` without `--remove-orphans`; both containers run image `sha256:5357be5...`, `gp` is healthy, restart counts are zero, `web` remained healthy, and the old image is recoverable as `gp-backend:rollback-c10be816`. The live worker requested only missing symbol `002036`; Sina and Tencent returned empty and Eastmoney disconnected, producing `requested=1`, `target_present=0`, `failed=1`, then exact `3043/3044` coverage and no second refresh within the observation window. This proves the old-date false-success bug is fixed, but also exposed a release blocker: the pending plan uses older fallback evidence and is rejected from publication as `stale_publication_write`, leaving the pre-deployment current recommendation visible. The database remained `contract_kernel.v1` with `integrity_check=ok`.

2026-07-24 runtime OCR/chat check: the live Serenity child committed a v2 exact batch for 30 symbols and 11 text-layer documents, then degraded safely on a later CNINFO 504 without restarting the core worker. A synthetic Chinese image-only announcement executed inside the running worker container as `tesseract_ocr` with Tesseract 5.3.0, 93 median confidence and 603 recognized characters. A disposable real `/api/chat` session successfully explained the current Top-3 and Serenity's fixed 3%/±0.03 product rule without OCR, process, database or interface details; the verification session was deleted and confirmed 404 afterward. Remaining risk is the current-publication invalidation gap above, so end-to-end fail-closed acceptance is not yet complete.

2026-07-24 suspension-cache repair: `python -m pytest -q` passed 52 tests, including default-CI coverage for missing sidecar, valid sidecar reuse, digest mismatch, capture-time expiry, disk-read TTL non-extension and repeated stale fallback. These cases prove neither a successful disk read nor a failed-source fallback can reset the evidence clock or be relabeled fresh by the process-level memory cache. `python -m compileall -q src tests`, contract self-check, manifest check, retired-symbol check, `docker compose config --quiet`, `git diff --check`, frontend lint/typecheck/14 tests/build all passed. The lock now carries both GNU and musl Rollup/SWC binaries; a separate Debian Node image pull stalled before execution, so the exact GNU install remains delegated to the PR's Ubuntu runner rather than being claimed as locally executed.

2026-07-24 final suspension runtime acceptance: built shared backend image `sha256:85b3dde...` with revision `0e2cfa2`; the default Debian mirror failed after retries on two 502 responses, then the same build succeeded through the configured Aliyun APT/PyPI mirrors. Recreated only `gp` and `gp-worker`; both report the exact revision, zero restarts, and `gp` is healthy while `web` remained unchanged. The new-container production predicate read a verified same-session Sina snapshot and returned raw main-board count 3044, exactly one no-bar symbol, and `contains_002036=true`; that row had `prev_close=7.23` with price/open/high/low/volume/amount all zero. Current plan `plan_969e750aa799e9444ccf2276` is `recommend`, binds 2026-07-24 daily evidence for 2026-07-27, records `total_count=3044`, `eligible_count=3043`, `complete=true`, and selects `600875/600550/600026`. Serenity correctly contributed 0% because its exact batch was unavailable. A real `/api/chat` response used this publication and explained the Top-3 and zero Serenity effect; the disposable session was deleted and returned 404. `schema_metadata=contract_kernel.v1` and `PRAGMA integrity_check=ok`.

## Lunch Top-30 Five-Minute Rerank

2026-07-24: `python -m pytest -q` passed 40 tests. The suite covers the exact 11:32 trigger, complete 30-symbol plus CSI300 batch gating, missing/duplicate/unfinished source failure, deterministic reranking, unchanged daily trade facts, Serenity's persisted fixed 3% bound, append-only plan/runtime/publication history, current-pointer CAS, late-publication prevention, real spawned-process write exclusion, current-schema migration refusal without byte changes, old/new conversation publication binding, afternoon preservation of the immutable lunch signal, product-only LLM context and output rejection when engineering identifiers or interfaces leak.

2026-07-24: `python -m pytest -q tests/test_akshare_minute_provider.py` passed 3 tests. `python -m pytest -q tests/integration/test_live_lunch_min5_source.py -m integration` passed 1 live read-only test through the isolated child-process collector. The live batch contained all 30 frozen candidates plus CSI300 with exactly 24 closed five-minute rows from 09:35 through 11:30.

2026-07-24: `python -m compileall -q src tests`, contract-manifest check, retired-symbol check, `docker compose config --quiet`, and `git diff --check` passed. Tests used temporary SQLite files; no production database, container, public response field, ContractModel, or schema version was changed or deployed.

2026-07-24: A final SQLite read-only compatibility audit of the live `contract_kernel.v1` store found zero content-ID, publication-decision, lineage, candidate, plan/runtime market-session, current-pointer, or foreign-key problems; `integrity_check` returned `ok`. This specifically confirmed that the PR retains the existing v1 publication reason semantics instead of silently invalidating historical rows.

## Serenity Fixed 3% Unified Worker

2026-07-24: `compileall`, the full 18-test contract suite, retired-symbol enforcement, Compose config, and diff checks passed. Tests prove positive, negative, and neutral complete-batch behavior; exact zero invariance when one symbol is missing or a source fails; the ±0.03 cap; stable zero-plan reuse and complete-batch plan invalidation; full evaluated-candidate retention with Serenity restricted to the frozen Top-30; prevention of uncovered candidates entering selection because a covered finalist was penalized; first-seen availability that never backdates evidence to the announcement clock; product-level public health without raw collector errors; and product-only LLM context without Serenity internal references.

2026-07-24: Rebuilt `gp-backend:local` and recreated `gp` and `gp-worker` from the same image `sha256:c10be816d0671792fcdb27d94ad7e8720af4cf8737750ad4c5471e4509f3aa41`. Container/local SHA-256 matched for both the Serenity service and conversation prompt. API health was healthy, the unified worker had separate core and Serenity processes, and the former `gp-gp-serenity-worker-1` container was removed by exact name. The historical 8.6 MB Serenity evidence database remained present.

2026-07-24: A live official-announcement poll built the current Top-30 target and encountered an unparsed `000100` PDF. Health exposed Serenity degradation, the committed recommendation used `adaptive_kernel_v3_serenity`, all 30 candidate Serenity weights were exactly 0, and the core worker subsequently published a new runtime/publication. A real chat explained the fixed 3%, signed bound, neutral complete case, and source-failure whole-batch zero in natural Chinese without exposing fields, reason codes, IDs, or interfaces; the disposable verification conversation was then deleted.

## Contract Kernel

2026-07-23: backend suite passed (5 tests); `compileall`, manifest, retirement, and diff checks passed. Frontend lint, typecheck, test (1 test), and production build passed.

The real local data path generated a 2026-07-23 plan from a live AkShare spot universe (3,194 main-board symbols) intersected with cached daily evidence (3,193 covered symbols, 2026-07-22). It evaluated 197 candidates and deterministically selected 3. A real `/api/chat` call completed through the configured LLM; a same-session/client-turn retry returned the persisted response without a duplicate turn. Outside market hours, the actual runtime refresh explicitly published `market_not_in_trading_phase` and `tradeable_now=false`.

The configured development database passed `integrity_check`, `foreign_key_check`, canonical initialization, and API health smoke after destructive replacement.

## Workspace Reconnection

2026-07-23: Restored the chat-first workspace against the current Contract Kernel. Added read-only canonical conversation list/detail routes; `tests/contracts` passed (6 tests), including an HTTP verification that a persisted session returns ordered canonical turns. Frontend `typecheck`, `lint`, test (1 test), and production build passed before container rebuild.

2026-07-23: Rebuilt the local Compose `gp`, `gp-worker`, and `web` services. Browser verification at `http://127.0.0.1:8080` showed the restored workspace, current candidate snapshot, session switching, persisted-turn reload, and a real LLM response correctly describing post-close state and 2026-07-22 daily evidence. No browser console errors were observed. A stale bind-mounted history lock from a replaced container was automatically reclaimed by the rebuilt worker; the expanded contract suite passed (7 tests) and no later worker lock timeout appeared.

## Chat Agent Showcase UI

2026-07-23: Recreated the missing frontend against current contracts and passed `npm run typecheck`, `npm run lint`, `npm test -- --run` (6 tests), and `npm run build`. The final production build emitted 155.57 kB JavaScript and 16.31 kB CSS before gzip. Cross-platform optional bindings for Rollup and SWC are explicitly locked so the Windows-generated lockfile builds reproducibly in the Alpine image. Tests cover canonical fact rendering, chat persistence, historical-publication isolation, state-sync failure closure, response-loss retry with stable session and client-turn identities, and Chinese IME composition.

2026-07-23: `docker compose config --quiet` passed. Plain `docker compose up -d --build` built the API and web images, then started `gp` healthy, `gp-worker` running, and `web` healthy. HTTP verification through `http://127.0.0.1:8080` returned 200 and proxied a current publication with 2026-07-23 daily evidence in post-close phase.

2026-07-23: Automated browser verification found 901 characters of meaningful initial content, no Vite error overlay, and no captured console errors. It rendered six saved conversations, three current selected candidates in engine order, and the post-close unavailable state. A new real chat produced and persisted a two-turn conversation; after page reload, selecting the newest saved session restored both ordered turns. The frontend exposed three candidate cards and two conversation messages after reload.

2026-07-23: After independent frontend review, historical sessions now hide the unrelated current decision brief, polling failure forces execution unavailable, uncertain retries reuse the original client-turn identity, IME composition Enter does not submit, and mobile history remains horizontally accessible. The final web-only rebuild used `docker compose up -d --no-deps --build web`; it did not rebuild or restart backend services. At a 390 px browser viewport, all six history buttons remained visible through the horizontal session rail. The final browser pass again found no console error or framework overlay.

2026-07-23: Repaired false historical-session detection caused by minute-level runtime publication churn. Frontend lint, typecheck, 10 Vitest cases, production build, and diff checks passed. The tests cover same-plan publication rollover, crossed health/publication reads, conversation request ordering, current/publication separation, Chinese reason rendering, evaluated-count wording, and execution-risk semantics. A web-only Compose rebuild left `gp` and `gp-worker` untouched. Browser verification reopened the reported `a7d44e66` session, correctly labeled its initially unknown lineage, sent a real follow-up, learned the trusted same-plan binding, restored all three current candidates, rendered `进入评分 198`, `执行风险`, and the Chinese next-session reason, and found no console errors or framework overlay.

2026-07-23: Added per-conversation deletion and passed backend compile, the full 8-test backend suite, frontend lint/typecheck, 14 Vitest cases, production build, Compose config, and diff checks. Tests cover exact session deletion, cascaded turn removal, preservation of another session and current plan/publication, tombstoned late-chat rejection, confirmation cancellation, active/inactive deletion, stale list suppression, and both deletion/switch response orders. Rebuilt `gp`, `gp-worker`, and `web`; all became healthy/running. Browser verification created only `session_delete_e2e_1784815569461`, deleted it through the first card's UI control, observed the list count move from 9 to 8, reloaded with the card still absent, received 404 from its conversation read and 409 `conversation_deleted` from a late same-ID chat, and captured no console error or framework overlay. No pre-existing user conversation was deleted.
## 2026-08-02 — 600439 official one-day suspension recognition

- Live read-only evidence: CNINFO record `1225449756`, published `2026-07-31T00:00:00+08:00`, passed SSE symbol/title verification and parsed as text. It states `600439` halted all day on 2026-07-31 and resumes on 2026-08-03 as `ST瑞贝卡`.
- Root-cause reproduction: the prior `_halt_excerpt` returned no evidence because the announcement says “停牌日期为2026年7月31日” and “停牌1天” without “开市/开盘”. The ledger therefore retained `target_date_missing` after 76 attempts.
- Pre-deployment checks: the original suspension-only slice passed 5 tests; after adding the weekend recovery regression, `python -m pytest -q tests/contracts/test_daily_refresh_exact_coverage.py` passed 6 tests. `python -m pytest -q` passed 52 tests before reconstruction; compilation and `git diff --check` passed. The live collector with the revised source returned `verified_suspended`, exact date `2026-07-31`, source `cninfo+sse`, and verification basis `sse_symbol_title`.
- Reconstruction discovery: the live ledger was still `retry_wait` on Sunday with a stale `2026-08-01T00:03:06+08:00` retry deadline. A regression now proves a Saturday tick schedules the due prior-session recovery while returning `closed` and leaving lunch/intraday paths unavailable.
- Final runtime: the rebuilt Sunday worker changed the 2026-07-31 run to `complete`, cleared retry/error fields, and stored `600439` as `excluded`, reason `official_suspension`, source `cninfo+sse`, record `1225449756`, exact effective date `2026-07-31`. Health then reported `next_plan_target.state=published`, market session `2026-08-03`, evidence date `2026-07-31`, plan `plan_3e561dd219b559c6d55393b5`, publication `publication_955d3b265a1e0d97b5098039`.
- Deployment parity: SHA-256 for `official_suspension.py` and `market_orchestrator.py` matched between the checkout, `gp`, and `gp-worker`; Compose reported backend healthy, worker running, and web healthy.
## 2026-08-04 daily anomaly routing

- `python -m pytest -q tests/contracts/test_daily_anomalies.py tests/contracts/test_daily_refresh_exact_coverage.py` passed (8 tests).
- `python -m pytest -q` passed (55 tests).
- The internal classifier maps provider empty/transport/payload failures to existing retryable run reasons and maps date-bound lifecycle facts to existing `excluded` rows; no public/API contract or raw historical data is changed.
