# Example environment file for docker-compose (.env)
## Copy to .env and edit values; NEVER commit real secrets

# Timezone
TZ=Asia/Shanghai

# LLM (DeepSeek)
LLM_BASE_URL=https://api.deepseek.com/v1
# 填写你的真实密钥（不要加引号）
LLM_API_KEY=sk-93a2c7ce8bde4cfeb1f9604049fb0469
CHAT_MODEL=deepseek-chat

# Data provider (akshare | local | official)
DATA_PROVIDER=akshare

# HTTP request timeout (seconds)
GP_REQUEST_TIMEOUT_SEC=60

# Strict real data only (no synthetic fallbacks)
STRICT_REAL_DATA=1

# Dynamic universe + mainline constraints
GP_MIN_AVG_AMOUNT=5e8      # 5-day avg amount lower bound
GP_NEW_STOCK_DAYS=60       # exclude IPO <= 60 days
GP_PRICE_MIN=2
GP_PRICE_MAX=500
GP_DYNAMIC_POOL_SIZE=50   # take top-N by current amount from snapshot
GP_RESTRICT_MAINLINE=1     # restrict picks within mainline groups
GP_MAINLINE_TOP_N=2        # top N industries/concepts
GP_MAINLINE_MODE=auto      # industry|concept|auto
GP_MAX_PER_INDUSTRY=2      # diversify: max N per industry/theme
